name: 七因子选股系统 - 页面重建

on:
  push:
    branches: [main]
    paths:
      - 'scripts/generate_page.py'
      - 'scripts/generate_page_safe.py'
      - 'data/seven_factor_latest.json'
      - 'data/seven_factor_*.json'
      - '.github/workflows/regenerate-page.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  regenerate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate latest data
        run: python3 - <<'PY'
          import json
          with open('data/seven_factor_latest.json', encoding='utf-8') as f:
              data = json.load(f)
          candidates = data.get('candidates', [])
          print(f"latest data OK: {len(candidates)} candidates")
          if not candidates:
              raise SystemExit('latest candidate pool is empty; abort page regeneration')
          PY

      - name: Generate page from existing latest data
        run: python3 scripts/generate_page_safe.py

      - name: Commit generated page only
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git diff --cached --quiet || git commit -m "auto: regenerate decision dashboard"
          git push
